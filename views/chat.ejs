<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>VibeSync</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css">
    <link rel="stylesheet" href="/style.css">
    <style>
        /* Background with a slightly greenish-white tone */
        body {
            background-color: #f0f8f4; /* Light greenish-white */
            color: #000; /* Ensure all text is black */
        }

        .chat-box {
            background-color: #f0f8f4; /* White background for chat box */
            border-radius: 10px;
            padding: 15px;
            max-height: 400px;
            overflow-y: auto;
        }

        .chat-container {
            background-color: #e6f2e6; /* Soft greenish-white for input area */
            padding: 10px;
            border-radius: 10px;
        }

        /* User message styling */
        .user-message {
            background-color: #d4edda; /* Light green similar to Aarogyam */
            color: #000; /* Black text */
            padding: 10px;
            border-radius: 10px;
            margin-bottom: 5px;
        }

        /* Bot response styling */
        .bot-message {
            background-color: #c3e6cb; /* Aarogyam green */
            color: #000; /* Black text */
            padding: 10px;
            border-radius: 10px;
            margin-bottom: 5px;
        }

        .intro h2, .intro p {
            color: #000; /* Ensure "Hi, I'm Aarogyam" and other text are black */
        }

        @media (max-width: 768px) {
            .container {
                width: 100%;
                padding: 0 15px;
            }
            .intro h2 {
                font-size: 1.5rem;
            }
            .intro p {
                font-size: 0.9rem;
            }
            #message {
                font-size: 0.9rem;
            }
        }
    </style>
</head>
<body>

    <div class="container">
        <div class="intro text-center text-md-start">
            <h2 style="font-weight: 700;">Hi, I'm Aarogyam Bot.</h2>
            <p style="font-family: 'Helvetica', sans-serif;">What can I help with?</p>
        </div>
        <div class="chat-box" id="chat-box">
            <!-- Messages will appear here -->
        </div>
        <div class="chat-container">
            <form id="chat-form" class="d-flex">
                <input type="text" id="message" class="form-control" placeholder="Ask Aarogyam" required>
                <button type="submit" class="btn btn-light rounded-pill ms-2"><i class="fas fa-paper-plane"></i></button>
            </form>
        </div>
    </div>

    <script src="/script.js"></script>
</body>
</html>
